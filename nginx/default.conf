server {
    listen 80;
    server_name _;

    # Serve the React static files
    location / {
        root   /usr/share/nginx/html;
        try_files $uri /index.html;
    }

    # Anything starting with /api goes to the backend container
    location /api/ {
        proxy_pass http://backend:8800/;
    }
}